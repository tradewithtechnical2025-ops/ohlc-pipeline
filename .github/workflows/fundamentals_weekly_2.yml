name: Fundamentals Weekly Part 2

on:
  schedule:
    # Sunday 10:00 AM IST IST
    - cron: '30 4 * * 0'
  workflow_dispatch:

jobs:
  fund-weekly-2:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install dependencies
        run: pip install httpx
      - name: Run fundamentals weekly part 2
        run: python pipeline.py fund_weekly_2
        env:
          UPSTOX_TOKEN: ${{ secrets.UPSTOX_TOKEN }}
          WORKER_URL:   ${{ secrets.WORKER_URL }}
          WORKER_TOKEN: ${{ secrets.WORKER_TOKEN }}
