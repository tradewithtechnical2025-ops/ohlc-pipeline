name: Fundamentals Weekly Part 3

on:
  schedule:
    # Sunday 2:00 PM IST IST
    - cron: '30 8 * * 0'
  workflow_dispatch:

jobs:
  fund-weekly-3:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install dependencies
        run: pip install httpx
      - name: Run fundamentals weekly part 3
        run: python pipeline.py fund_weekly_3
        env:
          UPSTOX_TOKEN: ${{ secrets.UPSTOX_TOKEN }}
          WORKER_URL:   ${{ secrets.WORKER_URL }}
          WORKER_TOKEN: ${{ secrets.WORKER_TOKEN }}
