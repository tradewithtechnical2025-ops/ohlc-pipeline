name: Fundamentals Weekly Part 7

on:
  schedule:
    # 12:00 PM IST Sun
    - cron: '30 6 * * 0'
  workflow_dispatch:

jobs:
  fund-weekly-7:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install dependencies
        run: pip install httpx
      - name: Run fundamentals weekly part 7
        run: python pipeline.py fund_weekly_7
        env:
          UPSTOX_TOKEN: ${{ secrets.UPSTOX_TOKEN }}
          WORKER_URL:   ${{ secrets.WORKER_URL }}
          WORKER_TOKEN: ${{ secrets.WORKER_TOKEN }}
